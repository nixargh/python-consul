language: python

python:
  - pypy-5.3.1
  - 3.5
  - 3.6

install:
  - pip install tox
  - pip install tox-travis

script:
  - tox

jobs:
  include:
    - stage: flake
      python: 3.6
      script:
        - tox -e flake8
      install: ignore
